services:
  workspace:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
      - /workspace/node_modules
    networks:
      - hsm-app-network
    env_file:
      - ../.env
    command: sleep infinity
